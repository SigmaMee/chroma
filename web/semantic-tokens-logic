

- The greyscale token with the least contrast with white
is the surface token.
- The one following is the surfaceVariant
- The greyscale token with the least contrast with black is the surfaceInverted token
- The greyscale token before that, is the surfaceInvertedVariant token
- The first greyscale token that passes AA compliance 4.5:1 with the surfaceVariant, is the textTertiary token
- The one after that is the textSecondary token
- The one after that is the textPrimary token
- The first greyscale token that passes AA compliance 4.5:1 with the surfaceInvertedVariant is the textTertiaryInverted token
- The one before that is the textSecondaryInverted
- The one before that is the textPrimaryInverted
- The first greyscale token that passes 3:1 AA compliance with surfaceVariant is the outline token
- The one after it is the outlineVariant token
- The first greyscale token that passes 3:1 AA compliance with the surfaceVariantInverted token is the outlineInverted token
- The before it is the outlineInvertedVariant token.

If the user selects AAA instead of AA, use 7:1 for text tokens and 4.5:1 for outline tokens instead.




Convention: before: a color that is lighter. after: a color that is darker. Example: for neutral200, neutral100 is before it and neutral300 is after it